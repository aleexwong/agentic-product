# Server-side secret — never use VITE_ prefix (that would embed it in the browser bundle)
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_key_here

# Non-secret feature flag — set to true to enable LLM semantic word matching in the UI
VITE_LLM_ENABLED=true
